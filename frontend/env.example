AGENT_BASE_URL=http://127.0.0.1:8000
# (선택) 프록시 버퍼링 이슈가 있으면 "직접 백엔드 SSE 호출"을 켤 수 있습니다.
# - Docker/배포 환경에서는 기본적으로 OFF를 권장합니다(주소/포트가 빌드 시점에 고정될 수 있음).
NEXT_PUBLIC_USE_DIRECT_BACKEND=false
NEXT_PUBLIC_AGENT_BASE_URL=http://127.0.0.1:8000
# 백엔드에서 ADMIN_API_KEY를 설정했다면, 프론트(Next 서버)에도 동일하게 설정하세요.
ADMIN_API_KEY=

